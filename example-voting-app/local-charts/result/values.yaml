# Default values for result-app.
# This is a YAML-formatted file.
# Declare name/value pairs to be passed into your templates.
# name: value
image:
  repository: lrochette/result
  #pullSecret: cfcr

postgresql:
  postgresUser: postgres
  postgresPassword: postgres

replicaCount: 1
