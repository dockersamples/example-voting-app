# Default values for worker.
# This is a YAML-formatted file.
# Declare name/value pairs to be passed into your templates.
# name: value
image: r.cfcr.io/perfect_pipeline/codefresh/example-voting-app-worker
tag: latest
